#ifndef PONTO_H
#define PONTO_H

// Prototipos de funcao
// (...)

// Definicoes de estruturas
struct point {
    double x; // campos que compoem o tipo agregado (registro)
    double y; // double x, y;
};
typedef struct point Point;

#endif // PONTO_H