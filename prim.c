#include <stdio.h> // Essa eh uma biblioteca-padrao da linguagem C

// Todo programa em C tem como ponto de entrada essa funcao main()
int main() {
    printf("Meu primeiro programa em C, yuhuu!\n");
    /* A proxima linha de codigo mostra que
    podemos colocar qualquer numero de espacos
    entre dois elementos de um comando */
    return          0;
}
